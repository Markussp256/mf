[package]
name = "container_traits"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { path="../utils" }

paste = "1.0.15"
nalgebra = { version = "0.34", optional = true }
num-traits= "0.2.15"
thiserror = "2.0.7"

[features]
nalgebra_support = ["dep:nalgebra"]
